/**
 * Sample React Native Realm Database App
 * https://aboutreact.com/example-of-realm-database-in-react-native/
 *
 * @format
 * @flow strict-local
 */

import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

import {
  Header,
  LearnMoreLinks,
  Colors,
  DebugInstructions,
  ReloadInstructions,
} from 'react-native/Libraries/NewAppScreen';

export default class App extends React.Component{

    render(){

        return(
            <View>
                <Text> ' Introducing Realm local storage' </Text>
            </View>
        );
    }
}
